$schema: https://deno.land/x/denon@2.4.7/schema.json
scripts:
  start:
    cmd: deno run --allow-run --allow-env --allow-read --allow-write scripts/build.js
    desc: run my app.ts file
watcher:
  match:
    - src/**/*.*
  exts:
    - js
    - mjs
    - css
